enum TaskPilotIdentity {
    /// The public name shown in the controller UI, notifications, and bundle metadata.
    static let displayName = "TaskPilot"

    /// The existing BetterDisplay name is kept so current virtual screens keep working.
    static let compatibilityAgentScreenName = "Orbit Agent Screen"

    static let liveScreenWindowTitle = "TaskPilot Live Screen"
    static let queueWindowTitle = "TaskPilot Queue & History"
}
